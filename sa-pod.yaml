apiVersion: v1
kind: Pod
metadata:
  namespace: devops
  name: newpod
  labels:
    app: myapp
    env: dev
spec:
  serviceAccount: mango
  containers:
  - name: con1
    image: nginx:latest
    ports:
    - containerPort: 80
